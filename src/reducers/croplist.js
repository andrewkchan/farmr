import * as types from "../constants/ActionTypes";

const initialState = {
    cropIds: [], //array of the crop IDs in the croplist
    currentPage: 0
};

export default function croplist(state = initialState, action) {
    switch (action.type) {
        
    }
}